#include <iostream>
using namespace std;
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n;
	cin >> n;
	int count = 0;
	for (int i = 1;; i += 2)
	{
		if (i <= n)
			count += i;
		else
			break;
	}
	cout<<count<<endl;
	return 0;
}
